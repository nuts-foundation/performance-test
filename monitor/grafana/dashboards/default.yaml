apiVersion: 1

providers:
  - name: dashboards
    type: file
    # <bool> allow updating provisioned dashboards from the UI
    allowUiUpdates: true
    options:
      path:  /etc/grafana/provisioning/dashboards